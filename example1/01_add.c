/* 01_add: addition
 * try: break main / next / print a / print sum
 * try: "set var a = 100" before the addition line, then check the result
 */
#include <stdio.h>

int main(void)
{
    int a = 7;
    int b = 35;
    int sum;

    sum = a + b;

    printf("%d + %d = %d\n", a, b, sum);
    return 0;
}
